import { Palette } from "../styles/palette.slint";

// Solar bold icons (SVG, white glyph — tint with `colorize`).
// Assets: assets/solar/ (from Iconify solar collection).

component SolarIcon inherits Image {
    in property <color> icon-color: Palette.text;
    in property <image> glyph;
    source: root.glyph;
    width: 14px;
    height: 14px;
    image-fit: contain;
    colorize: root.icon-color;
}

export component PlayIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--play-bold.svg");
}

export component PauseIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--pause-bold.svg");
}

export component SkipStartIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--skip-previous-bold.svg");
}

export component SkipEndIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--skip-next-bold.svg");
}

export component StepBackwardIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--rewind-back-bold.svg");
}

export component StepForwardIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--forward-bold.svg");
}

export component CloseIcon inherits SolarIcon {
    width: 12px;
    height: 12px;
    glyph: @image-url("../../../../assets/solar/solar--close-circle-bold.svg");
}

export component SpeakerIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--volume-bold.svg");
}

export component SpeakerMutedIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--volume-cross-bold.svg");
}

export component EyeLineIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--eye-bold.svg");
}

export component EyeOffLineIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--eye-closed-bold.svg");
}

export component LockClosedIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--lock-bold.svg");
}

export component LockOpenIcon inherits SolarIcon {
    glyph: @image-url("../../../../assets/solar/solar--lock-unlocked-bold.svg");
}
